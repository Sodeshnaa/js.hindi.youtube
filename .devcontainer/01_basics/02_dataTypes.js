"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // not in use as we are using nodejs, not browser

console.log(3
    +
    3) // code readability shld is highly preferred

console.log("Vipul Dubey");

// ECMAScript for documentation

let name = "sodeshna"
let age = 20
let isLoggedIn = false
let state = null

// number => 2 to power 53
// bigint
// string => " "
// boolean => true/false
// null => standalone value
// undefined =>
// symbol => unique

//object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
// type of null is an object and tht of undefined is undefined
